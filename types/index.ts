export type TUserPageTab = "HOME" | "TICKET" | "HISTORY" | "SETTINGS";

export type TParkingItem = {
  mall: string;
  location: string;
  amount: number;
  date: string;
  plate: string;
  duration: string;
};
